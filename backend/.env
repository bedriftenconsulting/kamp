# Server
PORT=8080

# PostgreSQL (preferred in hosted envs like Render)
# Example: postgres://user:pass@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://kampdb_user:oRyZ8FANGAAia9nyHWcSAiqkuj6eb0sM@dpg-d6ufvjua2pns739bjiag-a.oregon-postgres.render.com/kampdb?sslmode=require

# PostgreSQL fallback (used when DATABASE_URL is empty)
DB_HOST=localhost
DB_PORT=5433
DB_USER=admin
DB_PASSWORD=admin
DB_NAME=kampdb
DB_SSLMODE=disable

# CORS allowlist (comma-separated). Supports wildcard if configured in backend.
# Keep local frontends + Vercel previews.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080,http://localhost:8081,https://*.vercel.app
